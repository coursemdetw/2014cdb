
2bg5第八週考試摘要

壹、	題目
(第三題) (協同計分, 分組進行) 請在各組的雲端 dowiki 中,根據下列 40 個帳號與密碼, 新增對應的使用者帳號與密碼後, 將雲端網址登錄在 wiki.mde.tw 各組第八週頁面中, 並說明操作過程與心得後, 將心得整理成 pdf 後繳交到 course@mde.tw.

===

貳、	操作過程
這題是要新增帳密，也就是要編輯在conf下的users.auth.php，並且把帳密的格式改成 ” 帳號 : 密碼(亂碼處理) : 姓名 : 信箱 : 身分 ”，本題總共有40個指定帳密。
為了節省時間成本，我使用Python程式來處理帳密資料，並產生出所需的帳密資料格式做法如下:
首先，我將帳號和密碼分別存程兩個txt檔，讓Python程式讀入這兩的檔案，並改寫成所需要的格式(” 帳號 : 密碼(亂碼處理) : 姓名 : 信箱 : 身分 ”)，然後將產出的結果存檔，檔名為 ” users.auth.php” 。
最後再使用git add . 、 git commit -m 'My changes' 、 git push，將遠端的wiki更新。

===

2bg5第九週考試摘要

壹、	題目
(第三題 30%) 請在各組的雲端 dokuwiki 中, 新增帳號與密碼都是由 abc001 ~ abc399 字串所組成的 399 名用戶登入對應資料, 並將製作過程與驗證流程拍成 flv 後上傳到個人的 Vimeo 資料區, 並將連結放在個人第九週頁面.

===

貳、	操作過程
這題是要新增帳密，也就是要編輯在conf下的users.auth.php，並且把帳密的格式改成 ” 帳號 : 密碼(亂碼處理) : 姓名 : 信箱 : 身分 ”，本題總共有399個指定帳密。
為了節省時間成本，我使用Python程式來處理帳密資料，並產生出所需的帳密資料格式做法如下:
首先，這題的密碼與帳號相同，我將帳號存成txt檔，讓Python程式讀入這的檔案，並改寫成所需要的格式(” 帳號 : 密碼(亂碼處理) : 姓名 : 信箱 : 身分 ”)，然後將產出的結果存檔，檔名為 ” users.auth.php” 。
最後再使用git add . 、 git commit -m 'My changes' 、 git push，將遠端的wiki更新。

===

參、	連結
操作過程flv：https://vimeo.com/92630828

===

2bg5期中報告摘要

壹、	連結
http://wiki.mde.tw/doku.php?id=2014s2b:%E6%9C%9F%E4%B8%AD%E5%A0%B1%E5%91%8A:%E7%AC%AC%E4%BA%94%E7%B5%84

